:-  :~  navhome/'/docs/'
        next/'true'
        sort/'7'
        title/'=; "tissem"'
    ==
;>

# `=; "tissem"`

`[%tssm p=taco q=hoon r=hoon]`: combine a named and/or typed noun with the
subject, inverted.

## Expands to

```
=/(p r q)
```

## Syntax

Regular: _3-fixed_.

## Examples

```
~zod:dojo> =foo  |=  a=@
                 =/   b  1
                 =;   c=@  :(add a b c)
                 2
~zod:dojo> (foo 5)
8
```

