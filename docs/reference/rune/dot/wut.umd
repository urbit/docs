:-  :~  navhome/'/docs/'
        next/'true'
        sort/'2'
        title/'.? "dotwut"'
    ==
;>

# `.? "dotwut"`

`[%dtwt p=hoon]`: test for cell or atom with Nock `3`.

## Produces

`%.y` if `p` is a cell; otherwise `%.n`.

## Syntax

Regular: *1-fixed*.

## Examples

```
> .?(42)
%.n

> .?([42 43])
%.y
```
